<Page
    x:Class="yanshuai.ApiProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="14,16,14,4">
            <TextBlock x:Name="PageTitle" Text="API 配置"
                       FontSize="32" FontWeight="Light"/>
        </StackPanel>

        <TextBlock x:Name="PageSubtitle"
                   Grid.Row="1"
                   Text="选中条目后使用下方按钮操作"
                   FontSize="12" Opacity="0.5"
                   Margin="14,0,14,10"/>

        <ListView x:Name="ProfilesList" Grid.Row="2"
                  SelectionMode="Single"
                  SelectionChanged="ProfilesList_SelectionChanged">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="14,11"
                            RightTapped="ProfileItem_RightTapped"
                            Holding="ProfileItem_Holding">
                        <StackPanel>
                            <TextBlock Text="{Binding NameWithDefault}" FontSize="15"/>
                            <TextBlock Text="{Binding Model}" FontSize="13" Opacity="0.6"
                                       Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding Url}" FontSize="11" Opacity="0.45"
                                       TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="SetDefaultBtn" Icon="Favorite" Label="设为默认"
                              IsEnabled="False" Click="SetDefaultBtn_Click"/>
                <AppBarButton Icon="Upload" Label="导出备份" Click="ExportApi_Click"/>
                <AppBarButton Icon="Download" Label="从备份恢复" Click="ImportApi_Click"/>
            </CommandBar.SecondaryCommands>
            <AppBarButton x:Name="AddBtn" Icon="Add" Label="新增" Click="AddButton_Click"/>
            <AppBarButton x:Name="EditBtn" Icon="Edit" Label="编辑"
                          IsEnabled="False" Click="EditButton_Click"/>
            <AppBarButton x:Name="DeleteBtn" Icon="Delete" Label="删除"
                          IsEnabled="False" Click="DeleteButton_Click"/>
            <AppBarButton x:Name="ToggleAllBtn" Icon="SelectAll" Label="全选"
                          Visibility="Collapsed" Click="ToggleAllBtn_Click"/>
            <AppBarButton x:Name="DeleteSelectBtn" Icon="Delete" Label="删除选中"
                          Visibility="Collapsed" IsEnabled="False" Click="DeleteSelectedBtn_Click"/>
            <AppBarButton x:Name="CancelSelectBtn" Icon="Cancel" Label="取消"
                          Visibility="Collapsed" Click="CancelSelectBtn_Click"/>
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="SelectHint" Text="" FontSize="13" Opacity="0.7"
                               Margin="12,0,0,0" VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <AppBarButton Icon="Back" Label="" Click="BackButton_Click"/>
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
